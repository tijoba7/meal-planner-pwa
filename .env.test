# Fake Supabase credentials for unit tests.
# These satisfy the startup check in src/lib/supabase.ts without connecting to
# a real Supabase instance — actual API calls are mocked with vi.mock().
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=test-anon-key-for-unit-tests
